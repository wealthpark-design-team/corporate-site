# 認証設定（どのホスティング環境でも使用可能）
# Vercel、Kinsta、AWS等の環境変数設定画面で以下を設定

# 認証の有効/無効（本番環境でtrue推奨）
NEXT_PUBLIC_AUTH_ENABLED=true

# 認証パスワード（ビルド時に埋め込まれる）
NEXT_PUBLIC_AUTH_PASSWORD=your-secure-password-here

# --- Option 1: Middleware Basic認証用（サーバーサイド） ---
AUTH_ENABLED=true
BASIC_AUTH_USER=wealthpark
BASIC_AUTH_PASSWORD=your-secure-password-here